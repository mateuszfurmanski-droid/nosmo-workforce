{
  "name": "@nosmo/agency",
  "version": "1.0.25",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check enhancements.js && node tests/static-smoke.mjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.2.1",
    "openid-client": "^6.8.4",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
