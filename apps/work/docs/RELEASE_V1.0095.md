# NOSMO Work V1.0095

- Replaces availability broadcasts with one worker-owned live status.
- Adds explicit, one-time recruiter-safe Agency connections.
- Synchronises `Available`, `Busy`, and `Ready on date` with connected Agency workspaces.
- Removes the embedded Agency mode and collapses secondary Worker Card details.
- Keeps private documents outside the live status connection.
