# NOSMO Work V1.0097

- Rebuilds Documents as one private file library instead of several competing panels.
- Makes `Add files` the single primary document action.
- Keeps Nexus classification and approval visible only while files are being reviewed.
- Moves Agency document replies into a collapsed, deliberate secondary action.
- Replaces large CV tiles with compact create/import actions and a readable CV file list.
- Changes document categories from six cards to one compact category strip.
