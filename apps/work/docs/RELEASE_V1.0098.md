# NOSMO Work V1.0098

- Rebuilds Jobs as one continuous search-to-application workspace.
- Keeps one `Add job` action and removes the duplicate desktop menu and floating mobile button.
- Collapses location, distance, freshness and work type into `Search preferences`.
- Replaces nine competing status buttons with one compact status selector.
- Combines the saved-jobs heading, text filter and result count into one toolbar.
- Preserves live search, all statuses, application tracking and the employer directory.
